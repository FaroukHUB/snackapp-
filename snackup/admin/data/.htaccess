# 🔒 SÉCURITÉ: Bloquer tout accès web aux fichiers de données
# Protège admin-pin.json, orders.json, customers.json, etc.

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
